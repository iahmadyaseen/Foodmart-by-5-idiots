# ==========================================================
# FOOD MART - ONLINE GROCERY STORE APPLICATION (NEXT.JS + PRISMA ORM & NEONDB POSTGRESQL)
# Developed by: TEAM 5 IDIOTS
# ==========================================================

# 1. RUNTIME & SYSTEM REQUIREMENTS
Node.js >= 18.0.0
npm >= 9.0.0

# 2. CORE ARCHITECTURE & DEPENDENCIES
next >= 15.0.0
react >= 19.0.0
react-dom >= 19.0.0
prisma >= 5.22.0
@prisma/client >= 5.22.0
bcryptjs >= 3.0.2
jose >= 5.9.6
lucide-react >= 0.546.0
motion >= 12.23.24
html2canvas >= 1.4.1
jspdf >= 4.2.1

# 3. KEY FEATURES
- Next.js App Router Architecture with React Server & Client Components
- Prisma ORM Database with strict isolation (Zero Data Leaks)
- Full Grocery Catalog across 10 Primary Categories
- Customer Most Purchased / Top Selling Products with Admin Controls
- Shopping Cart & Multi-Step Checkout
- PDF Invoice / Receipt Generator
- Protected Customer Order History & Profile
- Admin Control Dashboard (ay8880625@gmail.com) for Orders, Catalog & Messages

# 4. COMMANDS
# Install dependencies: npm install
# Initialize DB:        npm run db:push
# Seed initial data:    npm run db:seed
# Start dev server:     npm run dev
# Production build:     npm run build
